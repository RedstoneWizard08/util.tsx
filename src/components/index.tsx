import { useIncrementalState } from "./useIncrementalState";
import { useLocalStorage } from "./useLocalStorage";

export { useIncrementalState };
export { useLocalStorage };

export default {
    useIncrementalState,
    useLocalStorage
};
